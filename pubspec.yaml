name: gg_messenger
description: GG Messenger - local offline-only Flutter app with on-device conversations.
publish_to: 'none'
version: 0.11.0+11

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
